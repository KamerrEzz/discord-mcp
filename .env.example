# Discord bot token from the Developer Portal (Bot > Token)
DISCORD_TOKEN=your-bot-token-here

# Default guild (server) ID — used when no guildId is passed per-call.
# Optional: if omitted, the bot resolves to the first guild it's in.
GUILD_ID=123456789012345678
